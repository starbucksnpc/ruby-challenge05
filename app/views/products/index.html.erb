<h1>Products</h1>

<% @products.each do |product| %>
  <div>
    <h2><%= link_to product.title, product_path(product) %></h2>
    <p><%= product.price %></p>
    <p><%= product.stock_quantity %></p>
  </div>
<% end %>
